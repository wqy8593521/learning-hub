# 死锁四条件 — 口语旁白
lesson 死锁四条件

step 经典死锁
  @0: 线程 T1 持有锁 A 等锁 B，线程 T2 持有锁 B 等锁 A，形成僵局。
  @0 L1: 两个线程互相等待对方释放，谁也无法继续，这就是死锁。
  @1: 死锁不会自动解开，除非有外部干预，比如杀掉其中一个线程。
  @1 L1: 生产环境死锁会导致相关功能完全卡死，必须预防或快速发现。
  @2: 经典例子：转账时两个账户各加一把锁，顺序不一致就容易死锁。
  @2 L1: 画图时两个线程各拿一把锁等另一把，箭头形成环就是死锁。
  @3: 数据库行锁死锁也是同样原理，InnoDB 会自动检测并回滚一个事务。
  @3 L1: Java 和数据库的死锁本质都是循环等待资源。

step 四条件
  @0: 死锁发生需要同时满足四个条件：互斥、持有并等待、不可剥夺、循环等待。
  @0 L1: 四个缺一不可，破坏任意一个就能预防死锁。
  @1: 互斥：资源同一时刻只能被一个线程占用，比如锁。
  @1 L1: 持有并等待：拿着一个锁的同时等另一个，不释放已有的。
  @2: 不可剥夺：已获得的锁不能被强行抢走，只能主动释放。
  @2 L1: 循环等待：T1 等 T2 的，T2 等 T1 的，形成环。
  @3: 面试背四条件，再说破坏策略：固定加锁顺序破坏循环等待最常用。
  @3 L1: tryLock 超时放弃可以破坏持有并等待。

step 检测与预防
  @0: 预防死锁最有效的是固定加锁顺序，所有线程都先锁 A 再锁 B。
  @0 L1: 这样就不会出现 T1 拿 A 等 B、T2 拿 B 等 A 的交叉情况。
  @1: tryLock 带超时，拿不到就释放已有锁并重试，避免无限等待。
  @1 L1: ReentrantLock 的 tryLock 是实现超时放弃的经典手段。
  @2: 银行家算法是理论上的资源分配策略，实际工程里较少直接使用。
  @2 L1: 工程上靠规范加锁顺序加超时放弃，简单有效。

step 排查工具
  @0: 线上怀疑死锁时，用 jstack 打印线程栈，搜索 BLOCKED 和 waiting to lock。
  @0 L1: jstack 会标出 Found one Java-level deadlock，直接指出哪两个线程。
  @1: 图形化工具如 VisualVM、Arthas 也能检测死锁，更直观。
  @1 L1: Arthas 的 thread -b 可以快速找到阻塞其他线程最多的那个。
  @2: 发现死锁后要么重启恢复，要么定位代码修复加锁顺序。
  @2 L1: 预防永远比排查更重要，代码评审时关注锁的获取顺序。
