# MySQL B+树索引 — 口语旁白
lesson MySQL B+树索引

step 树高决定IO
  @0: 磁盘按页读取，B+ 树每个节点对应一页，树越矮磁盘 IO 越少。
  @0 L1: 二叉树扇出小，千万数据要几十层，每次查询几十次 IO。
  @1: B+ 树非叶子只存索引键，一页能塞几百个指针，扇出大，三层就能存千万行。
  @1 L1: 一般三层 B+ 树，两次磁盘 IO 就能定位到叶子，性能稳定。
  @2: 面试结论：B+ 树矮胖，适合磁盘页存储，是 InnoDB 索引的物理结构。
  @2 L1: 对比 B 树：B+ 叶子链表方便范围扫描。

step 聚簇一行
  @0: InnoDB 主键索引的叶子节点直接存完整行数据，这叫聚簇索引。
  @0 L1: 数据和索引存在一起，按主键物理有序，主键查询最快。
  @1: 表必须有聚簇索引，没显式主键会自动选唯一非空列或隐式 rowid。
  @1 L1: 主键最好用自增整型，插入时顺序追加，避免页分裂。
  @2: 一张表只有一个聚簇索引，因为数据只能有一种物理排序方式。
  @2 L1: 理解聚簇是理解二级索引和回表的前提。

step 二级回表
  @0: 按 name 建的二级索引，叶子节点只存索引列值加主键 id。
  @0 L1: 用 name 查时先走二级索引找到 id，这叫索引查找。
  @1: 拿到 id 后再回聚簇索引查完整行，这叫回表，多一次 IO。
  @1 L1: 如果查询列都在二级索引里，覆盖索引就不用回表，更快。
  @2: 回表是二级索引的代价，所以联合索引要考虑覆盖，减少回表。
  @2 L1: EXPLAIN 里 Extra 显示 Using index 就是覆盖索引。
  @3: 面试经典题：为什么推荐主键查询、为什么联合索引要注意列顺序。
  @3 L1: 都和聚簇、二级、回表这条链路有关。

step 最左前缀
  @0: 联合索引 (a, b, c) 按 a、b、c 顺序排序，查询必须从最左列开始匹配。
  @0 L1: where a=1 and b=2 能用索引，where b=2 跳过 a 就用不上。
  @1: 范围查询后的列索引失效，比如 a>1 后面的 b、c 用不上。
  @1 L1: 设计联合索引时把区分度高、常查的列放左边。
  @2: 最左前缀是索引失效的第一大原因，面试必考。
  @2 L1: 记住：联合索引是一棵 B+ 树，不是三棵独立的树。
