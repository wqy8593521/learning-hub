# JVM 内存结构 — 口语旁白
lesson JVM 内存结构

step 线程私有
  @0: 每个线程独有程序计数器、虚拟机栈、本地方法栈，互不干扰。
  @0 L1: 程序计数器记录当前执行到哪条字节码，线程切换后恢复用。
  @1: 虚拟机栈存方法调用的栈帧：局部变量表、操作数栈、返回地址。
  @1 L1: 每次方法调用压入一个栈帧，返回时弹出。
  @2: 本地方法栈给 native 方法用，结构和虚拟机栈类似。
  @2 L1: StackOverflowError 是栈太深，OutOfMemoryError 是栈无法扩展。

step 堆与元空间
  @0: 堆是所有线程共享的，几乎所有对象实例都在堆里分配。
  @0 L1: 堆是 GC 的主要战场，分新生代和老年代。
  @1: 元空间存类的元数据，JDK8 后取代了永久代，用的是本地内存。
  @1 L1: 永久代在 JDK8 移除，因为大小难调，容易 OOM。
  @2: 堆 OOM 是最常见的，元空间 OOM 通常是加载了太多类。
  @2 L1: 直接内存 DirectByteBuffer 不在堆里，但也受 MaxDirectMemorySize 限制。

step 对象创建
  @0: new 对象时先检查类是否已加载，没有就先走类加载流程。
  @0 L1: 然后在堆上分配内存，指针碰撞或空闲列表取决于堆是否规整。
  @1: 分配后零值初始化，成员变量设为默认值 0 或 null。
  @1 L1: 接着设置对象头，最后执行构造方法 init。
  @2: TLAB 线程本地分配缓冲让大部分对象在栈上快速分配，减少同步。
  @2 L1: 对象创建五步：类检查、分配、零值、头、init，面试常考。
  @3: 逃逸分析可能让对象栈上分配或标量替换，但别依赖这个优化。
  @3 L1: 大部分对象还是在堆上。

step 引用类型
  @0: 强引用默认就是，只要可达 GC 不会回收。
  @0 L1: 软引用在内存不足时才回收，适合缓存。
  @1: 弱引用下次 GC 一定回收，ThreadLocal 的 key 就是弱引用。
  @1 L1: 虚引用用于跟踪对象被回收的通知，get 永远返回 null。
  @2: 四种引用强度：强、软、弱、虚，逐级变弱。
  @2 L1: 理解引用类型有助于理解 WeakHashMap 和 ThreadLocal 泄漏。
