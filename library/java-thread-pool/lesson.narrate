# Java 线程池 — 口语旁白
lesson Java 线程池

step 裸线程爆炸
  @0: 每个请求都 new Thread 会导致线程数失控，内存和上下文切换开销暴涨。
  @0 L1: 操作系统能承受的线程数有限，高并发下很快就把机器拖垮。
  @1: 线程创建销毁也不便宜，频繁 new 会让 CPU 大量浪费在调度上。
  @1 L1: 这就是裸线程模型在生产环境几乎不可用的原因。
  @2: 线程池的核心思想是复用固定数量的 worker 线程，任务来了排队执行。
  @2 L1: 用 ThreadPoolExecutor 统一管理，才是工程上的正确姿势。

step 四级扩容
  @0: 任务提交后，线程池先看当前 worker 数是否小于 corePoolSize。
  @0 L1: 不够就新建线程执行，直到达到核心线程数。
  @1: 核心线程满了，新任务进工作队列排队，不立刻创建新线程。
  @1 L1: 队列起到削峰填谷的作用，让突发流量先缓冲。
  @2: 队列也满了，才会扩到 maximumPoolSize，继续新建线程消化。
  @2 L1: 四级顺序是：扩核心、入队、扩最大、拒绝，背下来面试很加分。
  @3: 超过最大线程数还处理不过来，就触发拒绝策略。
  @3 L1: 理解这四步比死记参数更重要。

step 队列削峰
  @0: 洪峰来临时，任务先堆在队列里，worker 按自己的节奏逐个取出执行。
  @0 L1: 队列类型很关键：有界队列能保护内存，无界队列可能 OOM。
  @1: ArrayBlockingQueue 有界，LinkedBlockingQueue 可无界，SynchronousQueue 不存储直接交接。
  @1 L1: Executors 工厂方法里 newFixedThreadPool 用无界队列，生产环境慎用。
  @2: 合理设置队列容量和线程数，才能在吞吐和延迟之间取得平衡。
  @2 L1: 监控队列长度是线程池调优的第一指标。

step 拒绝反压
  @0: 线程池满负荷时触发拒绝策略，默认 AbortPolicy 直接抛 RejectedExecutionException。
  @0 L1: 调用方必须捕获处理，否则任务就丢了。
  @1: CallerRunsPolicy 让提交任务的线程自己执行，形成反压，拖慢上游。
  @1 L1: 适合不允许丢任务的场景，用背压保护系统不被压垮。
  @2: DiscardPolicy 静默丢弃，DiscardOldestPolicy 丢最老的任务腾位置。
  @2 L1: 生产环境推荐自定义拒绝策略：打日志、入死信队列或告警。
