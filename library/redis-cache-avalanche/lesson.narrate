# 缓存穿透与雪崩 — 口语旁白
lesson 缓存穿透与雪崩

step 穿透打穿
  @0: 查询一个根本不存在的数据，比如 id 为负数的商品，缓存永远没有。
  @0 L1: 每次请求都 miss，直接打到数据库，像穿透了缓存层。
  @1: 恶意攻击可以用大量不存在 key 把 DB 打垮。
  @1 L1: 穿透和击穿的区别：穿透是 key 不存在，击穿是热点 key 过期。
  @2: 解决思路：让不存在的数据也被缓存，或者提前拦截不存在的 key。
  @2 L1: 空值缓存和布隆过滤器是两大武器。

step 布隆挡刀
  @0: 布隆过滤器用多个哈希函数在位数组上标记，判断 key 可能存在或一定不存在。
  @0 L1: 说「不存在」就真不存在，说「可能存在」还要再查缓存和 DB。
  @1: 请求先过布隆，不存在的 key 直接返回，根本不到缓存和数据库。
  @1 L1: 布隆有误判率，可能把不存在的判成可能存在，但不会漏判。
  @2: 适合 key 空间大、恶意扫描多的场景，配合 Redis 使用。
  @2 L1: 布隆不能删元素，需要重建或加计数布隆。

step 雪崩来袭
  @0: 大量 key 在同一时刻过期，或者 Redis 实例宕机，缓存集体失效。
  @0 L1: 所有请求同时 miss，并发全部涌向 DB，像雪崩一样压垮后端。
  @1: 常见诱因：批量导入时设了相同 TTL，或者缓存集群整体故障。
  @1 L1: 雪崩影响面比击穿大得多，是整个缓存层的问题。
  @2: 三板斧：过期时间加随机抖动、集群高可用、限流降级兜底。
  @2 L1: 随机抖动让过期时间分散，避免同一秒集体失效。

step 三板斧
  @0: 穿透用布隆过滤器或缓存空值，挡掉不存在的 key。
  @0 L1: 空值缓存要设短 TTL，防止占用太多内存。
  @1: 雪崩加过期随机抖动，比如基础 TTL 加零到三百秒随机值。
  @1 L1: 还要做主从加哨兵或集群，避免单点挂全挂。
  @2: 击穿用互斥锁或逻辑过期，保证热点 key 失效时不打穿 DB。
  @2 L1: 穿透、击穿、雪崩三题一起背，面试 Redis 缓存必考。
