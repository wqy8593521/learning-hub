# 事务隔离级别 — 口语旁白
lesson 事务隔离级别

step 三种异常
  @0: 并发事务可能出三种读问题：脏读、不可重复读、幻读。
  @0 L1: 脏读是读到别的事务还没提交的数据，提交回滚你就白读了。
  @1: 不可重复读是同一事务内两次读同一行，结果不一样，因为别的事务改了。
  @1 L1: 幻读是两次范围读，行数变了，因为别的事务插入或删除了。
  @2: 脏读最严重，幻读影响范围查询，不可重复读影响单行重复读。
  @2 L1: 三种异常对应不同隔离级别的防护能力。

step 四级对照
  @0: SQL 标准四级：读未提交、读已提交、可重复读、串行化，越往下越严。
  @0 L1: MySQL InnoDB 默认可重复读 RR，Oracle 默认读已提交 RC。
  @1: 读未提交几乎不用，串行化性能太差，实际主要在 RC 和 RR 之间选。
  @1 L1: RC 每次 SELECT 都生成新 ReadView，RR 只在第一次 SELECT 生成。
  @2: 面试要背四级分别防什么：RU 都不防，RC 防脏读，RR 防脏读加不可重复读，串行化全防。
  @2 L1: MySQL RR 靠 MVCC 加间隙锁，也能在很大程度上防幻读。

step MVCC快照读
  @0: 普通 SELECT 是快照读，不加锁，靠 MVCC 读历史版本。
  @0 L1: 事务开始时生成 ReadView，记录当前活跃事务列表。
  @1: 每行有 trx_id 和 roll_pointer，通过 undo 链找到可见版本。
  @1 L1: 比对 trx_id 判断这个版本对我可不可见，不可见就沿 undo 往前找。
  @2: RC 每次读都新建 ReadView，所以能读到别的事务已提交的修改。
  @2 L1: RR 只在第一次读建 ReadView，所以同事务内重复读结果一致。
  @3: 当前读 SELECT FOR UPDATE 不走 MVCC，要加锁读最新数据。
  @3 L1: 快照读和当前读的区别是 InnoDB 面试高频题。

step 间隙锁
  @0: RR 级别下，范围查询加 next-key 锁，锁住记录之间的间隙。
  @0 L1: 目的是阻止别的事务在范围内插入，防止幻读。
  @1: 间隙锁之间不互斥，但和插入意向锁冲突，所以能挡插入。
  @1 L1: 代价是并发插入性能下降，RC 级别没有间隙锁。
  @2: 开发中如果不需要防幻读，可以降到 RC 减少锁冲突。
  @2 L1: 理解间隙锁是理解 RR 和 RC 行为差异的关键。
