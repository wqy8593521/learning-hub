# Redis 缓存击穿 — 口语旁白
lesson Redis 缓存击穿

step 热点过期
  @0: 热点 key 平时扛住大量流量，一旦 TTL 到期被 Redis 删掉，缓存就空了。
  @0 L1: 注意过期是删除 key，不是把值标成旧的，GET 直接返回 nil。
  @1: 热点失效的瞬间，所有请求都会 miss，同时涌向数据库。
  @1 L1: 这就是缓存击穿，和穿透、雪崩是三个不同的问题。
  @2: 击穿针对的是单个热点 key，雪崩是大批 key 同时失效。
  @2 L1: 先分清三个概念，面试才不会答混。

step 并发击穿
  @0: 一千个请求同时发现缓存 miss，都会去查数据库，形成并发风暴。
  @0 L1: 典型的 check-then-act 竞态：都判断没有，都去加载。
  @1: 数据库瞬间压力暴增，连接池打满，接口超时，甚至拖垮整个系统。
  @1 L1: 热点商品、热门文章过期时最容易踩这个坑。
  @2: 核心矛盾是：缓存失效后，谁来负责回源，能不能只让一个人回源。
  @2 L1: 解决方案都围绕互斥和逻辑过期两个思路。

step 互斥锁
  @0: 缓存 miss 后，用 SET key NX 抢锁，只有一个线程成功去查库写缓存。
  @0 L1: Redis 单线程保证 SET NX 原子性，其他线程等待或短暂重试。
  @1: 拿到锁的线程查 DB、写缓存、释放锁，后来的线程就能命中缓存了。
  @1 L1: 可以用 Redisson 的 tryLock，或者自己实现双重检查。
  @2: 互斥锁保证一致性，但等待的线程会有短暂延迟。
  @2 L1: 适合对数据一致性要求高的场景。

step 逻辑过期
  @0: 热点 key 永不过期，但在 value 里存一个逻辑过期时间字段。
  @0 L1: 发现逻辑过期仍返回旧值，同时异步开一个线程去更新缓存。
  @1: 用户永远能拿到数据，不会阻塞等待回源，可用性优先。
  @1 L1: 代价是短暂时间内可能读到过期数据，属于 CAP 里选 A。
  @2: 逻辑过期适合热点读多、允许短暂不一致的场景，比如首页推荐。
  @2 L1: 互斥锁和逻辑过期是击穿的两大经典方案，面试必背。
