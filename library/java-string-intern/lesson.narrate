# String 与常量池 — 口语旁白
lesson String 与常量池

step 字面量创建
  @0: 双引号字面量会直接放进字符串常量池，堆里只保留一份副本。
  @0 L1: 第一次出现时入池，之后相同字面量都复用池里那一个对象。
  @1: 两个引用变量可以指向常量池里的同一个字符串，两个引用一个对象。
  @1 L1: 这也是字面量创建字符串非常省内存的原因。
  @2: 对比来看，new String 会在堆上另外 new 一个对象，和字面量不共享。
  @2 L1: 面试经典题就是字面量和 new 的区别，记住池在堆里、可共享。

step new与intern
  @0: new String 会在堆上创建一个全新对象，哪怕内容跟池里一模一样。
  @0 L1: 这时候堆上和常量池各有一份看起来相同的字符串。
  @1: intern 方法会查常量池，如果已有相同内容就返回池里那个引用。
  @1 L1: 没有的话会把堆上的字符串放进池里，再返回池中的引用。
  @2: 如果池里已经有了，intern 直接返回已有对象，堆上那个变成可回收的孤儿。
  @2 L1: 所以 intern 常用于手动把字符串纳入常量池复用。
  @3: 最终效果是让更多引用指向常量池里的唯一副本。
  @3 L1: 大量 intern 能省内存，但常量池也不能滥用，否则占满堆空间。

step 不可变性
  @0: String 类被 final 修饰，创建后内部字符不能改，这就是不可变性。
  @0 L1: 任何看似修改的操作，比如拼接，其实都是生成了新对象。
  @1: hello 想变成 hello 加感叹号，原来的 hello 还在，旁边多了一个新对象。
  @1 L1: 老对象不会被原地改写，所以 String 可以安全当常量用。
  @2: 需要频繁修改请用 StringBuilder，它是可变的，适合循环拼接。
  @2 L1: String 线程安全，StringBuilder 在单线程拼接场景更高效。

step 拼接原理
  @0: 编译器会把 a 加 b 这种常量折叠优化成一个对象，运行时并不真的加两次。
  @0 L1: 所以静态拼接几乎和写一个 ab 字面量一样轻量。
  @1: 但在循环里用加号拼接就糟了，每次循环都会产生新的 String 垃圾对象。
  @1 L1: 对象数量随循环次数暴涨，GC 压力会像进度条一样往上飙。
  @2: 正确做法是用 StringBuilder，在循环外创建一次，内部 append 复用缓冲区。
  @2 L1: 大量拼接场景记住：循环里别用加号，用 StringBuilder。
