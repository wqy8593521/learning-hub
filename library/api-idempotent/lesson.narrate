# 接口幂等设计 — 口语旁白
lesson 接口幂等设计

step 重复请求
  @0: 用户连点提交、网络超时客户端重试、MQ 重复投递，都可能导致同一操作执行多次。
  @0 L1: 支付、下单、扣库存如果不幂等，会出现重复扣款等严重事故。
  @1: 幂等定义：同一个请求执行一次和执行多次，最终效果一样。
  @1 L1: 不是不让重试，而是重试不会产生副作用。
  @2: 先识别哪些接口必须幂等，再选合适的实现方案。
  @2 L1: 查询天然幂等，写操作才需要设计。

step 幂等Token
  @0: 提交前先调接口领取一次性 Token，服务端存 Redis 并设短过期。
  @0 L1: 正式提交时带上 Token，服务端校验并删除，第二次提交 Token 已失效。
  @1: 适合表单提交防重复，前端先拿 Token 再提交。
  @1 L1: Token 用完即作废，保证同一 Token 只处理一次。
  @2: 缺点是多一次请求，Token 过期要重新领取。
  @2 L1: 和 CSRF Token 不同，幂等 Token 防的是重复提交。

step 唯一键去重
  @0: 用业务单号或请求 ID 作数据库唯一索引，重复插入直接失败。
  @0 L1: 比如订单号、支付流水号，天然具有唯一性。
  @1: 先 insert，冲突捕获 DuplicateKeyException，说明已处理过，返回成功。
  @1 L1: 这是 DB 层兜底，最可靠，不怕应用层逻辑漏洞。
  @2: 唯一键方案简单有效，推荐作为最后一道防线。
  @2 L1: 配合状态机使用效果更好。

step 状态机
  @0: 订单状态单向流转：待支付→已支付→已发货，不能从已支付回到待支付。
  @0 L1: 更新时用 CAS：where status=待支付 set status=已支付，影响行数 0 说明已处理。
  @1: 重复支付请求第二次更新失败，直接返回已支付结果。
  @1 L1: 状态机加唯一键是支付类接口的标准组合拳。
  @2: 面试答幂等：Token 防前端重复、唯一键 DB 兜底、状态机 CAS 更新。
  @2 L1: 三层防护，越往下越可靠。
