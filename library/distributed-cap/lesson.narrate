# CAP 与一致性 — 口语旁白
lesson CAP 与一致性

step 分区来临
  @0: 分布式系统节点之间通过网络通信，网络随时可能分区断开。
  @0 L1: 分区 P 是客观存在的，宕机、丢包、光缆断了都算。
  @1: 分区发生时，C 一致性和 A 可用性无法同时满足，只能二选一。
  @1 L1: CAP 不是说平时不能同时有 CA，而是分区时被迫取舍。
  @2: 理解 CAP 的关键：P 不可避免，分区时 C 和 A 打架。
  @2 L1: 平时网络正常可以同时追求 CA，但设计要按分区最坏情况想。

step CP停服
  @0: CP 系统分区时宁可停服也要保证数据一致，比如 ZooKeeper、etcd。
  @0 L1: ZK 选主期间拒绝写入，客户端收到错误而不是脏数据。
  @1: 强一致性场景：配置中心、分布式锁、元数据管理。
  @1 L1: 代价是分区时部分节点不可用，牺牲可用性换一致。
  @2: 选 CP 意味着接受分区时服务降级，但数据不会乱。
  @2 L1: 金融核心、库存扣减等场景倾向 CP。

step AP继续
  @0: AP 系统分区时各节点继续服务，允许短暂数据不一致。
  @0 L1: Eureka 注册中心各节点独立接受注册，互相同步，可能读到旧列表。
  @1: 最终一致：过一会儿网络恢复，数据会收敛到一致状态。
  @1 L1: 大多数互联网业务选 AP，因为可用性直接影响用户体验。
  @2: 购物车、社交动态、DNS 等允许短暂不一致的场景适合 AP。
  @2 L1: 用版本号、向量时钟、冲突合并等手段做最终一致。

step 一致性光谱
  @0: 一致性不是只有强和弱两极，中间有读己之写、单调读、因果一致等。
  @0 L1: 强一致：写完立刻能读到最新；最终一致：过一会才能读到。
  @1: 实际业务大多在光谱中间找平衡点，不会极端 CP 或 AP。
  @1 L1: 比如主从复制异步，读从库可能滞后，就是弱一致。
  @2: 面试答 CAP 要加一句：BASE 理论，基本可用、软状态、最终一致。
  @2 L1: 业务决定选型，没有银弹，看能容忍什么不一致。
