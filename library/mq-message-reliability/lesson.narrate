# MQ 消息可靠性 — 口语旁白
lesson MQ 消息可靠性

step 三处会丢
  @0: 消息可能丢在三个环节：生产端、Broker 端、消费端。
  @0 L1: 生产发了没确认就以为成功，Broker 没刷盘就宕机，消费先 ACK 后处理失败。
  @1: 端到端可靠要三个环节都兜底，缺一不可。
  @1 L1: 只保证 Broker 不丢不够，生产和消费也要确认机制。
  @2: 可靠性设计和性能是权衡，同步确认更安全但更慢。
  @2 L1: 先画三阶段，再逐个讲方案。

step 生产确认
  @0: 同步发送等 Broker ACK，确认写入成功才返回，失败就重试。
  @0 L1: RocketMQ、Kafka 都有 acks 配置，all 表示所有副本确认。
  @1: 本地消息表：业务数据和消息在同一事务写入，定时任务扫表重发。
  @1 L1: 保证至少发一次，配合消费幂等实现精确一次语义。
  @2: 事务消息：半消息先发 Broker，本地事务成功再 commit，失败 rollback。
  @2 L1: RocketMQ 事务消息是经典方案。

step Broker刷盘
  @0: Master 收到消息后同步复制到 Slave，Master 挂了 Slave 顶上。
  @0 L1: Kafka ISR 机制，只有同步副本都写入才算 committed。
  @1: 刷盘策略：同步刷盘最安全，异步刷盘性能好但可能丢几条。
  @1 L1: 集群部署加多副本，单点故障不丢消息。
  @2: Broker 可靠性靠副本加刷盘加集群，生产至少三节点。
  @2 L1: 磁盘坏了靠副本，进程挂了靠持久化。

step 消费幂等
  @0: 消费端先处理业务逻辑，成功后再 ACK，失败不 ACK 会重新投递。
  @0 L1: 但重复投递不可避免，所以消费逻辑必须幂等。
  @1: 幂等手段：业务唯一键、数据库唯一索引、Redis 去重、状态机 CAS。
  @1 L1: 同一条消息处理两次，结果和处理一次一样。
  @2: 端到端可靠公式：生产确认加 Broker 持久化加消费幂等。
  @2 L1: 面试从三阶段展开，每阶段给一两个具体方案。
