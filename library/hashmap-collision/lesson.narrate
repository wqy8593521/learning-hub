# HashMap 冲突 — 口语旁白
lesson HashMap 冲突

step hash计算
  @0: put 时先算 key 的 hashCode，再经过扰动函数 h 异或 h 右移 16 位。
  @0 L1: 扰动是为了让高位也参与运算，减少哈希碰撞。
  @1: 不同 key 可能算出相同 hash，这就是哈希冲突。
  @1 L1: 冲突不可避免，HashMap 用链地址法解决。
  @2: 记住扰动公式和目的，面试常问为什么不让 hashCode 直接当索引。
  @2 L1: 因为数组长度通常比 hash 范围小，需要更好的分布。

step 定位桶
  @0: 用 hash 与数组长度减一做与运算，得到桶下标 index。
  @0 L1: 长度必须是 2 的幂，这样等价于取模但位运算更快。
  @1: 同一个 index 就是一个桶，桶里可能挂多个键值对。
  @1 L1: 理想情况每个桶只有一个节点，查找 O(1)。
  @2: 数组为空时先初始化，默认容量 16，负载因子 0.75 触发扩容。
  @2 L1: 0.75 是时间和空间的折中，太大冲突多，太小浪费内存。

step 链表冲突
  @0: 同桶已有节点时，新节点以头插法挂到链表头部，JDK7 如此。
  @0 L1: JDK8 改为尾插，避免多线程扩容时可能形成环的问题。
  @1: get 时遍历链表，用 equals 比对 key，找到就返回值。
  @1 L1: 链表越长查找越慢，所以冲突多了要扩容或树化。
  @2: 头插和尾插的区别是 JDK7 和 JDK8 的重要差异点，面试爱考。
  @2 L1: 多线程下 JDK7 头插扩容可能死循环，JDK8 已修复。

step 树化扩容
  @0: 单桶链表长度达到 8 且数组长度至少 64 时，链表转红黑树。
  @0 L1: 树化后查找从 O(n) 降到 O(log n)，应对哈希攻击。
  @1: 节点少于 6 时退化回链表，避免树维护开销。
  @1 L1: 扩容时数组翻倍，所有节点重新 hash 分散到新桶。
  @2: 扩容是新建两倍数组，逐个迁移，JDK7 头插可能成环，JDK8 尾插安全。
  @2 L1: 面试总结：扰动、取模、链表、树化、扩容，五步讲清 HashMap。
