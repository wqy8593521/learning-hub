# 类加载与双亲委派 — 口语旁白
lesson 类加载与双亲委派

step 加载三阶段
  @0: 类加载分加载、链接、初始化三大阶段。
  @0 L1: 加载是把 class 文件读入内存，生成 Class 对象。
  @1: 链接分验证、准备、解析：检查格式、静态变量赋零值、符号引用转直接引用。
  @1 L1: 初始化执行 clinit 方法，给静态变量赋真实值，执行静态代码块。
  @2: 主动使用类时才初始化，比如 new 实例、访问静态字段、反射。
  @2 L1: 子类初始化会先触发父类初始化，但接口不一定。

step 双亲委派
  @0: 类加载器收到请求，先委派给父加载器，父能加载就父加载。
  @0 L1: 父加载不了子才自己尝试，层层向上到 Bootstrap。
  @1: 保证 java.lang.String 等核心类由 Bootstrap 加载，不会被应用 classpath 里的同名类替换。
  @1 L1: 这是 Java 安全模型的基础，防止核心 API 被篡改。
  @2: 流程：App 委派 Ext，Ext 委派 Bootstrap，Bootstrap 加载不了往下尝试。
  @2 L1: 双亲委派是类加载面试第一题。
  @3: 加载器和加载的类不是同一个概念，一个类由哪个加载器加载很重要。
  @3 L1: 不同加载器加载的同名类在 JVM 看来是不同类型。

step 类加载器
  @0: Bootstrap 加载 rt.jar 核心库，C++ 实现，Java 里拿不到引用。
  @0 L1: Extension 加载 ext 目录扩展库，Application 加载 classpath 应用类。
  @1: 自定义 ClassLoader 继承 ClassLoader，重写 findClass 加载特定路径。
  @1 L1: 线程上下文类加载器用于 SPI，打破双亲委派从下往上找。
  @2: 三层加载器加自定义，理解各自加载范围是基本功。
  @2 L1: SPI 如 JDBC 驱动，接口在 Bootstrap，实现在 App，靠线程上下文类加载器桥接。

step 打破委派
  @0: SPI 需要子加载器加载实现类，所以线程上下文类加载器从子往父找。
  @0 L1: 这是打破双亲委派的经典场景。
  @1: Tomcat 为每个 Web 应用隔离类加载器，同名类可以共存。
  @1 L1: 热部署也是自定义 ClassLoader 重新加载 class。
  @2: 打破委派不是坏事，是为了解决特定场景的需求。
  @2 L1: 面试答打破场景：SPI、OSGi、Tomcat 隔离、热部署。
