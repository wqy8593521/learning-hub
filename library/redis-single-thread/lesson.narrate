# Redis 单线程模型 — 口语旁白
lesson Redis 单线程模型

step 事件循环
  @0: Redis 用单线程处理客户端命令，靠 IO 多路复用 epoll 监听多个连接。
  @0 L1: 事件来了就取出来执行，没有传统的一连接一线程模型。
  @1: 这种 Reactor 模式避免了线程切换和锁竞争，实现简单且高效。
  @1 L1: 命令执行是串行的，天然保证原子性，不用额外加锁。
  @2: 理解单线程加事件循环，是理解 Redis 性能和安全性的基础。
  @2 L1: 网络 IO 和命令执行在同一线程里顺序处理。

step 为何快
  @0: 第一，数据全在内存，访问速度比磁盘快几个数量级。
  @0 L1: 第二，底层数据结构经过精心优化，操作大多是 O(1) 或 O(log n)。
  @1: 第三，单线程没有锁和上下文切换，CPU 缓存命中率高。
  @1 L1: 第四，IO 多路复用让少量线程处理大量连接。
  @2: 所以 Redis 快不是因为多线程，而是因为内存、数据结构和模型简单。
  @2 L1: 别误以为单线程就慢，在内存场景下单线程往往更快。

step 6.0多线程IO
  @0: Redis 6.0 引入了多线程，但只用于网络 IO 的读写和协议解析。
  @0 L1: 命令的执行仍然在主线程，保证原子性不变。
  @1: 多线程 IO 解决的是大并发下网络读写成为瓶颈的问题。
  @1 L1: 默认可以不开，连接数不多时单线程 IO 够用。
  @2: 面试要说清：6.0 是多线程 IO，不是多线程执行命令。
  @2 L1: 执行模型还是单线程，这是和 Memcached 多线程的重要区别。

step 阻塞点
  @0: 单线程意味着一个慢命令会阻塞所有其他请求，比如 KEYS 或大 key 删除。
  @0 L1: 生产环境禁用 KEYS，用 SCAN 代替；大 key 用 UNLINK 异步删。
  @1: RDB 的 fork 和 AOF rewrite 也可能造成短暂卡顿。
  @1 L1: 持久化时 copy-on-write 会让内存翻倍，要注意实例内存规划。
  @2: 应用层避免慢命令、大 value、热 key 集中，是 Redis 调优的基本功。
  @2 L1: 单线程模型的代价就是一切都要快，慢的活别在 Redis 里干。
