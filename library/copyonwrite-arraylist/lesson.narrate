# CopyOnWrite 读写 — 口语旁白
lesson CopyOnWrite 读写

step 读无锁
  @0: CopyOnWriteArrayList 读操作直接访问内部数组，完全不加锁。
  @0 L1: 读线程之间、读写之间都不会互斥，吞吐量很高。
  @1: 适合读远远多于写的场景，比如监听器列表、配置快照。
  @1 L1: 读的时候拿到的是某一时刻数组引用的快照。
  @2: 代价是写操作比较重，所以名字里 CopyOnWrite 已经暗示了策略。
  @2 L1: 写少读多是用它的前提，写频繁千万别用。

step 写时复制
  @0: 写操作先加锁，复制一份新数组，在新数组上修改。
  @0 L1: 改完后用 volatile 把引用指向新数组，读线程立刻看到新数据。
  @1: 旧数组在没有任何引用后被 GC 回收，写期间读线程仍读旧数组。
  @1 L1: 这就是写时复制，读不加锁靠快照隔离。
  @2: 每次写都要复制整个数组，写操作是 O(n)，元素多时很慢。
  @2 L1: 所以只适合列表不大、写入极少的场景。
  @3: 加锁保证同一时刻只有一个写线程，避免两个写同时复制。
  @3 L1: 写完后一次性切换引用，对读者是原子可见的。

step 弱一致迭代
  @0: 迭代器创建时快照当前数组，迭代过程中看不到之后的写入。
  @0 L1: 这叫弱一致性，不是实时视图，但绝不会抛 ConcurrentModificationException。
  @1: 不会抛 CME 是因为迭代器和写操作操作的是不同数组副本。
  @1 L1: 如果需要强一致遍历，CopyOnWrite 不合适。
  @2: 监听器通知场景正好需要：遍历时就算有人增删监听也不影响当前通知。
  @2 L1: 这是它最经典的应用场景。

step 适用场景
  @0: 读多写极少，比如黑名单配置、事件监听器列表。
  @0 L1: 写一次复制全数组可以接受，读性能优先。
  @1: 写频繁或列表很大时，每次复制开销不可接受，应换 ConcurrentHashMap 或其他结构。
  @1 L1: 别看到线程安全就无脑用 CopyOnWrite，先评估读写比例。
  @2: 面试结论：读无锁写复制，弱一致迭代，适合读多写少小列表。
  @2 L1: 和 Collections.synchronizedList 对比：后者读也要加锁。
