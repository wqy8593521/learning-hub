# TCP 三次握手 — 口语旁白
lesson TCP 三次握手

step 第一次SYN
  @0: 客户端想建立连接，发送 SYN 包，序号 seq 设为随机值 x。
  @0 L1: 客户端进入 SYN_SENT 状态，等待服务端确认。
  @1: SYN 表示请求同步序号，双方要先对齐初始序号才能可靠传输。
  @1 L1: 随机 seq 是为了安全，防止历史连接被误用。
  @2: 第一次握手只带了 SYN，还没有确认号。
  @2 L1: 服务端收到后知道客户端想连接，但还不知道客户端收包能力。

step 第二次SYN-ACK
  @0: 服务端回复 SYN 加 ACK，自己的序号 seq 设为 y，确认号 ack 为 x 加 1。
  @0 L1: 服务端进入 SYN_RCVD 状态，半连接队列里挂着这个连接。
  @1: SYN 表示服务端也要同步序号，ACK 表示确认收到了客户端的 SYN。
  @1 L1: 第二次握手服务端确认了客户端的发送能力。
  @2: 但此时服务端还不知道客户端能不能收到自己的包。
  @2 L1: 所以还需要第三次握手。

step 第三次ACK
  @0: 客户端收到 SYN-ACK 后，发送 ACK，确认号 ack 为 y 加 1。
  @0 L1: 客户端进入 ESTABLISHED，服务端收到后也进入 ESTABLISHED。
  @1: 双方都确认了对端的发送和接收能力，连接建立完成。
  @1 L1: 第三次握手客户端可以带数据，但通常不带。
  @2: 三次握手完成，双方序号对齐，可以开始可靠传输数据。
  @2 L1: 记住每步谁确认了什么，是画图的关键。

step 为何三次
  @0: 两次握手的问题：服务端发了 SYN-ACK 就认为连接建立了，但客户端可能没收到。
  @0 L1: 客户端没收到就不会发 ACK，服务端白白占着半连接资源。
  @1: 三次握手让双方都确认对方能收能发，避免无效连接。
  @1 L1: 也防止已失效的旧连接请求被服务端误当成新连接。
  @2: 四次没必要，三次已经能确认双向通信能力，是最小必要次数。
  @2 L1: 面试答：确认双方收发能力、同步序号、防历史连接。
