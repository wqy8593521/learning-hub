# 行锁与间隙锁 — 口语旁白
lesson 行锁与间隙锁

step 行锁Record
  @0: InnoDB 的行锁加在索引记录上，不是加在行数据本身。
  @0 L1: 没有索引时可能退化为表锁，所以更新必须走索引。
  @1: 精确匹配命中唯一索引时，只锁那一行记录。
  @1 L1: 当前读如 SELECT FOR UPDATE、UPDATE、DELETE 都会加行锁。
  @2: 行锁之间互斥，不同行不冲突，所以并发更新不同行可以并行。
  @2 L1: 锁的是索引项，理解这点才能理解为什么没索引会锁全表。

step 间隙锁Gap
  @0: 间隙锁锁住两个索引记录之间的空隙，防止范围内插入新行。
  @0 L1: 只在 RR 级别存在，RC 没有间隙锁。
  @1: 比如 id 在 5 和 10 之间有间隙锁，别的事务不能插入 id=7。
  @1 L1: 间隙锁之间不互斥，多个事务可以同时持有同一间隙的间隙锁。
  @2: 目的是防幻读，但会降低并发插入能力。
  @2 L1: 范围查询 where age between 20 and 30 可能锁很多间隙。

step Next-Key
  @0: Next-Key Lock 等于行锁加间隙锁，锁住记录本身和前面的间隙。
  @0 L1: 默认加锁单位，左开右闭区间，是 InnoDB 最常用的锁。
  @1: 扫描索引时，走过的每个 next-key 都会加上，直到不满足条件。
  @1 L1: 这就是为什么范围更新可能锁很多行甚至间隙。
  @2: 理解 record、gap、next-key 三者的关系，是 InnoDB 锁面试的核心。
  @2 L1: 降级到 RC 可以去掉间隙锁，只保留行锁。

step 死锁检测
  @0: 多个事务以不同顺序加 next-key 锁，可能形成循环等待死锁。
  @0 L1: InnoDB 有死锁检测，发现环就回滚代价最小的事务。
  @1: 应用层收到死锁错误可以重试，但要控制重试次数和间隔。
  @1 L1: 预防死锁：固定访问顺序、缩短事务、避免大事务。
  @2: show engine innodb status 可以看最近一次死锁日志。
  @2 L1: 行锁死锁和 Java 死锁本质相同，都是循环等待。
