# RDB 与 AOF — 口语旁白
lesson RDB 与 AOF

step RDB快照
  @0: RDB 是定时 fork 子进程，把内存数据快照写成压缩的二进制文件 dump.rdb。
  @0 L1: fork 用 copy-on-write，主进程继续服务，子进程写快照。
  @1: 恢复快，直接加载 rdb 文件到内存，适合灾难恢复和冷备份。
  @1 L1: 缺点是两次快照之间的数据可能丢失，取决于 save 配置。
  @2: save 900 1 表示 900 秒内至少 1 次修改就触发，可以配多条规则。
  @2 L1: 也可以手动 BGSAVE 或 SHUTDOWN SAVE。

step AOF日志
  @0: AOF 把每条写命令追加到日志文件，重启时重放命令恢复数据。
  @0 L1: 比 RDB 丢数据少，但文件更大，恢复更慢。
  @1: appendfsync 三个级别：always 每条刷盘最安全但慢，everysec 每秒刷盘折中，no 交给 OS。
  @1 L1: 生产常用 everysec，最多丢一秒数据。
  @2: AOF 文件会膨胀，需要 BGREWRITEAOF 压缩，用重写生成最小命令集。
  @2 L1: 重写也是 fork 子进程，和 RDB 类似有短暂内存压力。

step 混合持久化
  @0: Redis 4.0 起推荐 RDB 加 AOF 混合模式，AOF 文件前半段是 RDB 快照。
  @0 L1: 后半段是增量 AOF 命令，兼顾恢复速度和数据完整性。
  @1: 重启时先加载 RDB 部分快速恢复大部分数据，再重放少量 AOF。
  @1 L1: 比纯 AOF 重放快很多，比纯 RDB 丢得少。
  @2: 生产环境开 aof-use-rdb-preamble yes，这是当前最佳实践。
  @2 L1: 混合持久化是 RDB 和 AOF 的取长补短。

step 选型
  @0: 数据可以丢几分钟，纯缓存场景，RDB 够用，配置 save 规则即可。
  @0 L1: 尽量少丢数据，开 AOF everysec，接受一定的 IO 开销。
  @1: 生产推荐混合持久化，再加主从复制做热备份。
  @1 L1: 主从解决的是实例故障，持久化解决的是进程重启后数据还在。
  @2: 面试答：RDB 快但可能丢，AOF 少丢但慢，混合加主从是生产标配。
  @2 L1: fork 阻塞是大实例要关注的性能点。
