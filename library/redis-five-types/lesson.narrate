# Redis 五大数据结构 — 口语旁白
lesson Redis 五大数据结构

step String
  @0: String 是 Redis 最基础的结构，二进制安全，可以存文本、数字甚至图片。
  @0 L1: SET 和 GET 都是 O(1)，常用来做缓存、计数器、分布式锁。
  @1: INCR 原子自增适合做点赞数、库存扣减，不用应用层加锁。
  @1 L1: SET NX EX 组合是分布式锁的经典写法。
  @2: 单个 value 最大 512MB，但实际业务别存太大的 value，会阻塞单线程。
  @2 L1: String 看似简单，面试能展开到缓存、锁、计数三个场景。

step Hash
  @0: Hash 存字段和值的映射，适合把一个对象的多个属性放在一起。
  @0 L1: HSET user:1 name Tom age 20，改单个字段不用序列化整个对象。
  @1: 每个字段的读写都是 O(1)，比存 JSON 字符串更省内存也更灵活。
  @1 L1: 字段数不多时 Hash 比多个 String key 更紧凑。
  @2: 典型场景：用户信息、商品属性，需要部分更新时用 Hash。
  @2 L1: 字段特别多时要注意大 Hash 的渐进式 rehash 开销。

step List
  @0: List 是双向链表，两端插入弹出都是 O(1)。
  @0 L1: LPUSH 加 RPOP 就是 FIFO 队列，RPUSH 加 LPOP 也成立。
  @1: 可以做简单消息队列、最新动态时间线、任务列表。
  @1 L1: BRPOP 阻塞弹出，适合消费者等待新消息。
  @2: 中间查找是 O(n)，所以 List 不适合随机访问，只适合头尾操作。
  @2 L1: 复杂队列场景更推荐 Stream 或专业 MQ。

step Set与ZSet
  @0: Set 是无序去重集合，SADD 和 SISMEMBER 都是 O(1)。
  @0 L1: 适合标签、共同好友、抽奖去重等需要判重的场景。
  @1: ZSet 在 Set 基础上每个成员带一个 score，按分数排序。
  @1 L1: ZRANGE 取排行榜前 N 名，ZRANK 查排名，非常高效。
  @2: 五种结构选型：缓存用 String，对象用 Hash，队列用 List，去重用 Set，排行用 ZSet。
  @2 L1: 面试按场景选结构，比背命令更重要。
