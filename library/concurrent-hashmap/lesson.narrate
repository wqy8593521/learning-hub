# ConcurrentHashMap — 口语旁白
lesson ConcurrentHashMap

step 分段锁
  @0: JDK7 的 ConcurrentHashMap 把哈希表切成多段，每段一把独立的 ReentrantLock。
  @0 L1: 不同线程改不同段时可以并行写，只有命中同一段才会互斥。
  @1: 读操作大多不加锁，靠 volatile 保证可见性，get 性能接近 HashMap。
  @1 L1: 这也是它比 Hashtable 全表 synchronized 快得多的根本原因。
  @2: 面试要记住：JDK7 是分段数组加段锁，粒度比整张表一把锁细很多。
  @2 L1: 段数默认 16，并发度可以初始化时指定。

step CAS无锁读
  @0: get 方法全程不加锁，直接读 volatile 的 Node 数组和链表。
  @0 L1: 写操作才需要加锁或 CAS，读写分离是高性能的关键。
  @1: 可见性靠 volatile 和 happens-before 保证，读者能看到最新写入。
  @1 L1: 不会出现读到一半更新的中间态，因为引用替换是原子的。
  @2: size 统计用 LongAdder 分散计数，避免所有线程抢同一把锁。
  @2 L1: 高并发下 size 是近似值，需要精确值可以调 mappingCount。

step JDK8桶锁
  @0: JDK8 取消了 Segment，结构和 HashMap 一样，数组加链表或红黑树。
  @0 L1: 锁粒度进一步细化到单个桶的头节点，并发度更高。
  @1: 空桶插入用 CAS 抢头节点，失败才 synchronized 锁住桶头。
  @1 L1: 这种乐观加悲观的组合，在无竞争时几乎零开销。
  @2: 链表转红黑树时仍用 synchronized 锁桶，保证树化过程线程安全。
  @2 L1: JDK8 之后面试重点从分段锁转向 CAS 加 synchronized 桶锁。

step 扩容协助
  @0: 扩容时多个线程可以一起帮忙迁移桶，不是单线程搬完全表。
  @0 L1: 每个线程认领一段区间，搬完再认领下一段，避免全表阻塞。
  @1: 迁移用 forwardingNode 标记已搬走的桶，其他线程看到会帮忙或重定向。
  @1 L1: 这是 JDK8 ConcurrentHashMap 最精巧的设计之一。
  @2: 相比 HashMap 单线程扩容，高并发写入时停顿感更小。
  @2 L1: 面试结论：JDK8 桶锁加多线程协助扩容，读基本无锁。
