# 并发工具类 — 口语旁白
lesson 并发工具类

step CountDownLatch
  @0: CountDownLatch 像倒计时门闩，初始设成 N，每完成一个任务 countDown 减一。
  @0 L1: 主线程调 await 阻塞，直到计数归零才放行。
  @1: 典型场景：等所有子线程干完活，主线程再汇总结果。
  @1 L1: 比如启动时等五个服务都初始化完毕，再对外提供服务。
  @2: 注意它是单次使用的，计数归零后不能重置，要复用得用 CyclicBarrier。
  @2 L1: countDown 可以在任意线程调，不限于子线程自己。

step CyclicBarrier
  @0: CyclicBarrier 让一组线程互相等待，都到齐了才一起继续。
  @0 L1: 像赛跑运动员到起跑线，最后一个到了大家才开跑。
  @1: 可以设置 barrierAction，到齐后由最后一个线程执行一段回调。
  @1 L1: 和 CountDownLatch 不同，它是可循环复用的，适合多轮并行计算。
  @2: 典型场景：并行计算分片，每轮算完在屏障处合并，再开始下一轮。
  @2 L1: 某个线程 await 超时或中断，屏障会 broken，其他线程抛异常。

step Semaphore
  @0: Semaphore 是信号量，控制同时访问某资源的线程数量。
  @0 L1: 初始化许可数比如 10，表示最多 10 个线程并发。
  @1: acquire 拿一个许可，没有就阻塞；release 归还许可。
  @1 L1: 常用于限流、连接池、停车位模型。
  @2: 可以一次 acquire 多个许可，也可以 tryAcquire 非阻塞尝试。
  @2 L1: 公平模式按 FIFO 发放许可，非公平允许插队。

step 选型对比
  @0: 等子任务全部完成用 CountDownLatch，一个等多个。
  @0 L1: 多个线程互相等多轮齐步用 CyclicBarrier。
  @1: 限制并发访问数量用 Semaphore，像停车场限车位。
  @1 L1: 三者底层都基于 AQS，但语义和使用场景完全不同。
  @2: 面试快速区分：Latch 单向等待、Barrier 双向齐步、Semaphore 限流。
  @2 L1: 别混用，选错工具会让代码难以理解。
