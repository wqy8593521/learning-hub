# 锁升级机制 — 口语旁白
lesson 锁升级机制

step 锁状态机
  @0: synchronized 的锁信息存在对象头的 Mark Word 里，随竞争程度逐级升级。
  @0 L1: 升级路径是：无锁、偏向锁、轻量级锁、重量级锁，不可降级。
  @1: 无锁状态就是普通对象，第一次有线程访问才可能升级。
  @1 L1: JVM 会根据运行时的竞争情况自动选择最合适的锁形态。
  @2: 竞争加剧就升级，目的是在无竞争时尽量减少同步开销。
  @2 L1: 面试画对象头加四级锁状态机，基本就能讲清楚 synchronized 优化。
  @3: 注意锁升级是 JVM 内部优化，对开发者透明，写代码仍用 synchronized 即可。
  @3 L1: 了解原理有助于理解为什么 synchronized 在高竞争下会变慢。

step 偏向锁
  @0: 只有一个线程反复进入同步块时，JVM 在 Mark Word 记录该线程 ID。
  @0 L1: 同一线程再次进入时，发现 ID 匹配，直接通过，几乎零开销。
  @1: 偏向锁适合锁总是被同一个线程获取的场景，比如单线程下的 StringBuffer。
  @1 L1: 一旦有第二个线程来竞争，偏向锁就失效，需要撤销偏向。
  @2: 撤销偏向需要安全点停顿，所以多线程竞争下偏向锁反而有额外成本。
  @2 L1: JDK15 之后偏向锁默认关闭，因为现代应用多线程场景更普遍。

step 轻量锁
  @0: 第二个线程来竞争时，偏向锁升级为轻量级锁，用 CAS 尝试抢锁。
  @0 L1: 抢成功的线程在栈上创建 Lock Record，Mark Word 指向它。
  @1: 抢失败的线程不会立刻阻塞，而是在原地自旋，期待锁很快释放。
  @1 L1: 自旋适合锁持有时间很短的场景，避免线程阻塞唤醒的开销。
  @2: 如果自旋多次仍拿不到锁，说明竞争激烈，需要升级为重量级锁。
  @2 L1: 自旋次数由 JVM 自适应调整，不是固定的。

step 重量锁
  @0: 自旋超时后锁膨胀为重量级锁，由操作系统的 Monitor 接管。
  @0 L1: 未获锁的线程进入等待队列，真正阻塞，不再消耗 CPU 自旋。
  @1: Monitor 依赖操作系统 mutex，阻塞和唤醒涉及用户态内核态切换。
  @1 L1: 这是 synchronized 在高竞争下性能下降的原因。
  @2: 重量级锁保证公平性和正确性，适合锁持有时间较长的场景。
  @2 L1: 面试结论：锁升级是 JVM 优化手段，竞争少时快，竞争多时稳。
