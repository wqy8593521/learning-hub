# JVM 分代 GC — 口语旁白
lesson JVM 分代 GC

step 分代布局
  @0: 堆分新生代和老年代，新生代放新对象，大部分朝生夕灭。
  @0 L1: 新生代又分 Eden 和两块 Survivor，默认比例 8:1:1。
  @1: 老年代放存活时间长的对象，和元空间存类信息是不同区域。
  @1 L1: 分代假设：绝大多数对象活不过第一次 GC。
  @2: 分代让 GC 可以针对性回收，不用每次扫整个堆。
  @2 L1: 新生代用复制算法，老年代用标记清除或标记整理。

step Minor GC
  @0: Eden 满了触发 Minor GC，也叫 Young GC，只回收新生代。
  @0 L1: 标记 Eden 和 Survivor 中存活的对象，复制到另一个 Survivor。
  @1: 每熬过一次 GC 年龄加一，默认 15 次进老年代。
  @1 L1: 复制算法快，但有一个 Survivor 总是空的，浪费一点空间。
  @2: Minor GC 频繁但快，因为新生代小。
  @2 L1: 如果 Minor GC 后存活对象太多放不下 Survivor，会提前晋升老年代。

step 标记清除
  @0: 老年代 GC 先从 GC Roots 遍历，标记所有可达对象。
  @0 L1: GC Roots 包括栈引用、静态变量、JNI 引用等。
  @1: 标记完后清除未标记的对象，产生内存碎片。
  @1 L1: 碎片多了大对象可能找不到连续空间，触发 Full GC。
  @2: 标记整理在清除后把存活对象压缩到一端，消除碎片但移动对象更慢。
  @2 L1: CMS 用标记清除，G1 和 ZGC 用更先进的算法。

step G1 Region
  @0: G1 把堆切成大小相等的 Region，不再物理分代但逻辑仍分代。
  @0 L1: 每个 Region 可以是 Eden、Survivor 或 Old，动态角色。
  @1: 回收时优先选垃圾最多的 Region，用最少时间回收最多垃圾。
  @1 L1: 可以设目标停顿时间，G1 尽量在预算内完成。
  @2: G1 是 JDK9 默认 GC，适合大堆和低延迟需求。
  @2 L1: 面试对比：Serial 单线程、Parallel 吞吐、CMS 低延迟有碎片、G1 可预测停顿。
